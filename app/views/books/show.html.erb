<div>
  <%= flash[:notice] %>
</div>

<div class="container">
  <div class="row">
  <div class="col-md-3"><%= render 'list', book: @book, user: @user %>
  </div>

<div class="col-md-8 offset-md-1">
<h1>Book detail</h1>
<table class="table">
  <thead>
  <tr>
    <td><%= image_tag @book.user.get_profile_image(100,100) %>
    <br><%= link_to @user.name, user_path(@user) %></td>
 <td><%= link_to @book.title, book_path(@book.id) %></td>
 <td><%= @book.body %></th>
<td><% if @book.user == current_user %></th>
<td><%= link_to 'Edit' , edit_book_path(@book.id), class: "btn btn-sm btn-success" %></td>
<td><%= link_to 'Destroy' , book_path, method: :delete, "data-confirm" => "本当に削しますか？", class: "btn btn-sm btn-danger"  %></td>

<tbody>
   <tr><td></td></tr>
    
<% end %>
</tbody>
</table>
</div>
</div>